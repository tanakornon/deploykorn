{
  "name": "deploykorn",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "An idle/incremental game about deploying services, scaling a fleet and rebooting for permanent cores.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "check:ladder": "node scripts/check-upgrade-ladder.mjs",
    "check:progression": "tsx scripts/check-progression.ts",
    "probe:value": "tsx scripts/probe-upgrade-value.ts"
  },
  "dependencies": {
    "@fontsource-variable/inter-tight": "^5.3.0",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "astro": "^7.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "^26.6.1",
    "tsx": "^4.20.6",
    "typescript": "^6.0.3"
  }
}
